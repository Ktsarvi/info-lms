// The courses page uses the same Navbar as the homepage,
// but passes a `user` prop so it shows the profile instead of "Начать".
// This file simply re-exports it for clarity.
export { default } from "@/components/homepage/navbar";
